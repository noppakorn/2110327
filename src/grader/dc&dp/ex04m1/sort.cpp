#include <bits/stdc++.h>
using namespace std;
int n;
vector<int> v;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n;
    v = vector<int>(n);
    for (int i = 0; i < n; ++i) {
        cin >> v[i];
    }
}
