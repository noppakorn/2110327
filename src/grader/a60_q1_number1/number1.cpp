#include <bits/stdc++.h>
#define ll long long
using namespace std;


ll solve(ll n, ll l, ll r) {
    if ()

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    ll n, l, r;
    cin >> n >> l >> r;
    --l;
    cout << solve(n, l, r) << endl;
}