#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--) {
        int n, e;
        cin >> n >> e;
        bool check = false;
        cout << (check ? "YES\n" : "NO\n");
    }
}